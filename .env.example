# Upstox API credentials (https://upstox.com/developer/apps)
UPSTOX_API_KEY=
UPSTOX_API_SECRET=
UPSTOX_REDIRECT_URI=
# Access token generated via the Upstox OAuth login flow (valid for the trading day it was issued)
UPSTOX_ACCESS_TOKEN=
